
from itertools import combinations_with_replacement

a = input().split(' ')

#for i in range(1, int(a[1]) + 1):
for j in combinations_with_replacement(sorted(a[0]), int (a[1])):
    connector = ""
    joined_string = connector.join(j)
    print(joined_string)
